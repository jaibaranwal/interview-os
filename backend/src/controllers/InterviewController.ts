import { Request, Response } from 'express';

// Future responsibility: Express HTTP Controller handling POST /api/interview request parsing and response delivery
export class InterviewController {
  public async handleInterview(req: Request, res: Response): Promise<void> {
    // Controller entrypoint
  }
}
